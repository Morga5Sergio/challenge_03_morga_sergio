import 'package:flutter/material.dart';

class Sreen4 extends StatelessWidget {
  const Sreen4({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Image.asset("images/school.png")
    );
  }
}
